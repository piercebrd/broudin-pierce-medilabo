spring:
  cloud:
    gateway:
      routes:
        - id: patient-service
          uri: http://patient-service:8080  # Correct Docker service name
          predicates:
            - Path=/api/patients/**
          filters:
            - StripPrefix=1